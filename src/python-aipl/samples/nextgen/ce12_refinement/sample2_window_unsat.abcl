// CE-12 / sample 2 — UNSAT refinement (window 0<x<0).
//
// Run with:
//   AIPL_REFINE_CHECK=1 python3 aipl_main.py \
//     samples/nextgen/ce12_refinement/sample2_window_unsat.abcl
// Expected: refinement warning flags the impossible window.

function vacuous(x: int where x > 0 and x < 0) -> int {
  return x;
}

print("[ce12/2] declared (still compiles; --infer flags it)");
