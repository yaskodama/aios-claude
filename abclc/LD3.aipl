class Ponger {
  var cnt = 0.;
  method ping(x, y) {
    cnt = cnt + x + y;
    call print("pong: " + cnt);
  }
}

var ponger = new Ponger();
class Pinger {
  method start() {
    send ponger.ping(1, 2);
    send ponger.ping(3, 4);
  }
}

var pinger = new Pinger();