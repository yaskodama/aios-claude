class Fork {
  var taken = 0;

  method take() {
    if (taken == 0) {
      taken = 1;
    } else { print(""); 
    }
  }

  method release() {
    taken = 0;
  }
}

class Philosopher {
  var id = 0;
  var leftFork = nil;
  var rightFork = nil;

  method think() { print(""); }

  method hungry() { print(""); }

  method eat() { print(""); }
}

var fork0 = new Fork();
var fork1 = new Fork();
var fork2 = new Fork();
var fork3 = new Fork();
var fork4 = new Fork();
