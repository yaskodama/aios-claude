% !TEX program = xelatex
% AIPL Runtime Feature Matrix — 7 implementations compared

\documentclass[10pt,a4paper,landscape]{article}

\usepackage[a4paper,landscape,margin=15mm]{geometry}
\usepackage{xeCJK}
\usepackage{fontspec}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{makecell}
\usepackage{hyperref}
\usepackage{enumitem}

\setCJKmainfont{Hiragino Mincho ProN}
\setCJKsansfont{Hiragino Sans}
\setCJKmonofont{Hiragino Sans}
\setmonofont{Menlo}[Scale=0.85]

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue!60!black,
  pdftitle={AIPL Runtime Feature Matrix},
  pdfauthor={Yasushi Kodama}
}

% Status cells — colored backgrounds for readability
\newcommand{\Yes}{\textcolor{green!50!black}{\textbf{\checkmark}}}
\newcommand{\No}{\textcolor{red!70!black}{$\times$}}
\newcommand{\Part}{\textcolor{orange!80!black}{$\triangle$}}
\newcommand{\NA}{\textcolor{gray}{--}}

% Column types for tight matrix
\newcolumntype{C}{>{\centering\arraybackslash}p{0.9cm}}
\newcolumntype{F}{>{\raggedright\arraybackslash}p{6.5cm}}
\newcolumntype{N}{>{\centering\arraybackslash}p{0.6cm}}

\title{\textbf{AIPL Runtime Feature Matrix} \\
  {\large 7 implementations compared}}
\author{Yasushi Kodama}
\date{2026-05-13}

\begin{document}
\maketitle

\section*{Overview}

AIPL (Actor-based Intelligent Parallel Language, formerly AIPL)
has grown into a \textbf{seven-runtime project}.  This document
provides a side-by-side comparison of every language and
infrastructure feature across all implementations, grounded in
source surveys.

\paragraph{Legend.}
\Yes\ = full support, \Part\ = partial, \No\ = not supported,
\NA\ = not applicable.

\paragraph{The seven runtimes.}

\begin{tabularx}{\linewidth}{l X}
  \toprule
  \textbf{Short name} & \textbf{Source} \\
  \midrule
  Python (annotated)        & \texttt{src/python-aipl/} \\
  Python (inferred)         & \texttt{src/python-aipl-inferred/} \\
  OCaml                     & \texttt{src/*.ml}, \texttt{\_build/.../repl\_thread.exe} \\
  JS-OCaml (server)         & \texttt{src/app.js}, \texttt{console\_server.js}, \texttt{ide.js} + OCaml \texttt{web\_gateway} \\
  JS-Browser (serverless)   & \texttt{src/browser-abcl/} \\
  JS-Node (server)          & \texttt{src/node-aipl-server/} (Node HTTP server wrapping browser-abcl) \\
  C (aipl2c)                & \texttt{src/aipl2c.ml} codegen + \texttt{src/abcl\_gui\_runtime.c} \\
  \bottomrule
\end{tabularx}

\paragraph{Column abbreviations in matrices below.}
\textbf{Py-A}~=~Python annotated, \textbf{Py-I}~=~Python inferred,
\textbf{OCaml}, \textbf{JS-O}~=~JS via OCaml backend,
\textbf{JS-B}~=~JS in browser, \textbf{JS-N}~=~JS in Node server,
\textbf{C}~=~aipl2c codegen.

\paragraph{Inheritance relationships.}

\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item \textbf{JS-OCaml} is a thin HTTP client over the OCaml
    backend, so its feature set inherits OCaml's (plus the new
    \texttt{/api/typecheck} endpoint).
  \item \textbf{JS-Node} wraps the same browser-abcl parser /
    type-checker / interpreter inside a Node.js HTTP server.
    Runtime feature set therefore mirrors JS-Browser.
  \item \textbf{Python (inferred)} imports parser, interpreter, AI,
    remote, and dashboard from \texttt{../python-aipl/}.  All
    \emph{runtime} features match Python (annotated); only the
    \emph{static type-checker} layer differs (full
    Hindley-Milner instead of Phase 11+ annotation-driven).
\end{itemize}

\clearpage

\section*{Sample programs}

Number of \texttt{.abcl} sample programs reachable by each runtime
(core directory + AI integration + remote-actor demos):

\begin{tabularx}{\linewidth}{l C C C C C C C}
  \toprule
  & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  core samples           & 33   & 33   & 57   & 57   & 5    & 5    & 57   \\
  AI integration samples & 11   & 11   & 8    & 8    & 0    & 0    & 8    \\
  remote-actor samples   & 8    & 8    & 1    & 1    & 0    & 0    & 1    \\
  \midrule
  \textbf{total}         & \textbf{52} & \textbf{52} & \textbf{66} & \textbf{66} & \textbf{5} & \textbf{5} & \textbf{66} \\
  \bottomrule
\end{tabularx}

\smallskip

\noindent
Cross-cutting / not tied to a specific runtime:
\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item \texttt{aipl-self-host/}: 48 AIPL-in-AIPL self-host programs
  \item \texttt{docker/cross/samples/}: 4 cross-language interop samples
\end{itemize}

Note that the C runtime processes the same \texttt{.abcl} files as
OCaml via \texttt{aipl2c}; of the 66 reachable samples, 48 currently
pass the \texttt{aipl2c} smoke test (the remaining 9 in \texttt{abclc/}
have pre-existing type ambiguities surfaced by our hard-fail policy).

\subsection*{What each sample exercises}

Samples are grouped by the language or infrastructure feature they
demonstrate.  ``Where'' points to the source directory;
``Target'' lists the runtime(s) and codegen targets that actually
execute the sample.

\smallskip

\noindent\small
Target abbreviations: \textbf{Py}~=~python-aipl /
python-aipl-inferred; \textbf{OCaml}~=~OCaml REPL (\texttt{abclc});
\textbf{C}~=~\texttt{aipl2c}~$\to$~C+pthread;
\textbf{SDL2}~=~\texttt{aipl2c}~$\to$~C+SDL2 GUI binary;
\textbf{Xinu}~=~\texttt{aipl2c~--xinu}~$\to$~Xinu-flavoured C;
\textbf{Py-gen}~=~\texttt{aipl2c~--python}~$\to$~stand-alone Python;
\textbf{Pony}~=~\texttt{aipl2c~--pony}~$\to$~Pony actor source;
\textbf{Erlang}~=~\texttt{aipl2c~--erlang}~$\to$~Erlang module;
\textbf{Go}~=~\texttt{aipl2c~--go}~$\to$~Go source;
\textbf{Prolog}~=~\texttt{aipl2c~--prolog}~$\to$~SWI-Prolog \texttt{.pl};
\textbf{JS-B}~=~browser-abcl; \textbf{JS-N}~=~node-aipl-server.
\normalsize

\begin{longtable}{p{3.0cm} p{5.5cm} p{2.8cm} p{6.4cm} p{4.0cm}}
  \toprule
  \textbf{Feature category} & \textbf{Sample(s)} & \textbf{Where} & \textbf{What it checks} & \textbf{Target} \\
  \midrule
  \endhead

  Basic actor model & \texttt{Hello}, \texttt{Counter}, \texttt{PingPong} & py-aipl + abclc & actor creation, \texttt{send}, field state, \texttt{self}-send & Py, OCaml, C, JS-B, JS-N \\
  \texttt{now}/\texttt{future}/\texttt{await} & \texttt{NowFuture}, \texttt{CooperativeNowFuture}, \texttt{NowFutureDemo} & py-aipl + samples-ai + abclc/ai-samples & three message-passing forms in one program & Py, OCaml, C, JS-B, JS-N \\
  Bounded mailboxes / select & \texttt{BoundedBuffer}, \texttt{bounded\_buffer\{,\_visual\}} & py-aipl + abclc + browser-abcl & producer/consumer, selective receive, visualisation & Py, OCaml, C, JS-B, JS-N \\
  Dining philosophers & \texttt{Philosophers}, \texttt{philosophers\{-1,-2\}}, \texttt{Philosophers5\{,\_debug,\_trace,Gui,Py,Xinu\}} & py-aipl + abclc + browser-abcl & fork as actor, deadlock-free serialisation, SDL/Python/Xinu variants & Py, OCaml, C, SDL2, Py-gen, Xinu, JS-B, JS-N \\
  \texttt{become} (class swap) & \texttt{become}, \texttt{bbecome} & abclc & runtime actor-class replacement & Py, OCaml \\
  \texttt{select} / selective recv & \texttt{Channels}, \texttt{Channels2} & py-aipl/samples & typed channels, worker pool with request/result & Py \\
  Method injection & \texttt{MethodPatch} & py-aipl/samples + abclc/ & \texttt{add\_method}/\texttt{remove\_method} runtime patching & Py, OCaml \\
  Dynamic compile & \texttt{Dynamic}, \texttt{DynamicWorkerPool} & py-aipl/samples + abclc/ & \texttt{compile()} builtin $\to$ runtime class generation & Py, OCaml \\
  Top-level functions & \texttt{Functions}, \texttt{Signatures} & py-aipl/samples + abclc/ & user functions, overload signatures, typeof & Py, OCaml \\
  Records & \texttt{Records} & py-aipl/samples + abclc/ & \texttt{\{a:int, b:string\}} literal, field access, structural typeof & Py, OCaml \\
  Tuples & \texttt{Tuples} & py-aipl/samples + abclc/ & positional, immutable, mixed slot types, nesting & Py, OCaml \\
  Arrays & \texttt{Arrays}, \texttt{MultiDimArrays} & py-aipl/samples & static-sized, multi-dim, dynamic sizes & Py \\
  Gradual type checker & \texttt{Typecheck}, \texttt{Typecheck11\{b,c,de\}} & py-aipl/samples & Phase 11 $\to$ 11e: literals, call-site validation, unions/generics, narrowing, length-tagged arrays & Py \\
  Phase 11 typed counter & \texttt{Phase11\_TypedCounter} & abclc & typed annotations / generics / typeof narrowing & OCaml \\
  Phase 12 effects & \texttt{Effects}, \texttt{Phase12\_EffectsLog} & py-aipl + abclc & capability-based \texttt{!\{fs,ai,net,mut\}} system & Py (static), OCaml (sample) \\
  Phase 13 channels & \texttt{Phase13\_Channels} & abclc & CSP channels (design doc + runtime in Py) & Py (runtime), OCaml (sample) \\
  Phase 14 linear types & \texttt{Linear}, \texttt{Linear2}, \texttt{Phase14\_Linear} & py-aipl + abclc & use-after-move; DB transaction with linear handle & Py (static), OCaml (sample) \\
  Phase 15 owned fields & \texttt{Owned}, \texttt{Owned\_violations}, \texttt{Phase15\_Owned} & py-aipl + abclc & \texttt{pub} field visibility, intentional violations & Py (static), OCaml (sample) \\
  Phase 16 transient cast & \texttt{Transient}, \texttt{Transient\_violation} & py-aipl/samples & runtime type cast at any-boundary & Py \\
  Phase 17 structured conc. & \texttt{Phase17\_StructuredConc} & py-aipl/samples & \texttt{scope \{ future ... \}} auto-joining & Py \\
  Session types / protocol traces & \texttt{SessionTyped} & py-aipl/samples-ai & runtime-checked typed session protocols (arg + reply types); order-only protocol traces; AIOS service registry & Py \\
  AI integration (mock + real) & \texttt{AIActor}, \texttt{AIChain}, \texttt{AIChainReal}, \texttt{AIHello}, \texttt{MultiProvider} & py-aipl + samples-ai + abclc/ai-samples & LLM-backed actors; multi-provider; chained pipeline & Py, OCaml \\
  AI governance & \texttt{Budgeted} & py-aipl/samples-ai + abclc/ai-samples & token budget, concurrency cap, fallback chain & Py, OCaml \\
  AI cooperative pattern & \texttt{CooperativeNowFuture\{,-jp,-jp-remote\}}, \texttt{CooperativeSolve\{,-jp,Remote,Remote-jp\}}, \texttt{Reviewer}, \texttt{Fanout}, \texttt{PriorityFanout} & py-aipl/samples-ai + abclc/ai-samples & Planner$\to$Solver$\to$Reviewer; fan-out aggregator; priority routing & Py, OCaml \\
  Remote actors & \texttt{client}, \texttt{server}, \texttt{coordinator}, \texttt{solver}, \texttt{verifier}, \texttt{reviewer\_node*}, \texttt{RemoteCalcClient}/\texttt{Server} & py-aipl/samples-remote + abclc/samples-remote + abclc/ai-samples & HTTP cross-machine sends; HMAC-signed coordination & Py, OCaml \\
  Web / dashboard & \texttt{web\_calc}, \texttt{SiteGen} & abclc + py-aipl/samples & embedded HTTP gateway; static-site generator & Py (SiteGen), OCaml (web\_calc) \\
  GUI / SDL2 & \texttt{Rotate\{One,Three,Four\}Lines\{,Gui\}}, \texttt{MultiLineSpin}, \texttt{Philosophers5Gui}, \texttt{BoundedBufferGui}, \texttt{DisasterReturnGui}, \texttt{LineDrawer}, \texttt{window} & abclc & SDL2-backed GUI codegen via aipl2c & SDL2 (via aipl2c) \\
  Python codegen target & \texttt{BoundedBufferPy}, \texttt{Philosophers5Py}, \texttt{Rotate4LinesPy} & abclc & \texttt{aipl2c --python} emits stand-alone Python & Py-gen \\
  Xinu (embedded OS) target & \texttt{BoundedBufferXinu}, \texttt{Philosophers5Xinu}, \texttt{Rotate4LinesXinu} & abclc & \texttt{aipl2c --xinu} emits Xinu-flavoured C & Xinu \\
  Pony codegen target & \texttt{Hello}, \texttt{counter} (verified); \texttt{PingPong} (xfail) & abclc & \texttt{aipl2c --pony} emits Pony source; \texttt{class} $\to$ \texttt{actor}, methods $\to$ \texttt{be}; two-step \texttt{\_aipl\_init} decouples construction from init body & Pony \\
  Erlang codegen target & \texttt{Hello}, \texttt{counter} (verified); \texttt{PingPong} (xfail) & abclc & \texttt{aipl2c --erlang} emits an \texttt{.erl} module; \texttt{class} $\to$ \texttt{spawn} + \texttt{receive} loop; fields $\to$ loop args with versioned variables on assign & Erlang \\
  Go codegen target & \texttt{Hello}, \texttt{counter} (verified); \texttt{PingPong} (xfail) & abclc & \texttt{aipl2c --go} emits a single \texttt{main.go}; \texttt{class} $\to$ struct + goroutine; methods $\to$ typed message structs + helper methods + type-switch dispatch & Go \\
  Prolog codegen target & \texttt{Hello}, \texttt{counter} (verified); \texttt{PingPong} (xfail) & abclc & \texttt{aipl2c --prolog} emits a single SWI-Prolog \texttt{.pl} using \texttt{library(thread)}; \texttt{class} $\to$ \texttt{c\_loop(Fields)} thread + \texttt{thread\_get\_message} + \texttt{Msg = m(Args) -> body} dispatch; expressions hoisted to goals (\texttt{X is A + B}, \texttt{format(atom(S), ...)}) & Prolog \\
  LLVM codegen target & \texttt{Hello}, \texttt{counter} (verified) & abclc & \texttt{aipl2c --llvm} emits the default C with clang-specific \texttt{\_\_attribute\_\_((hot))} / \texttt{((cold))} on the actor / watchdog / print paths, plus a header banner listing \texttt{clang -O2 -pthread} (native), \texttt{clang -emit-llvm -S} (text IR), and \texttt{clang -emit-llvm -c + lli} (interpret) workflows & LLVM \\
  OpenMP codegen target & \texttt{Hello}, \texttt{counter} (verified) & abclc & \texttt{aipl2c --openmp} emits C + \texttt{<omp.h>}; the initial actor-spawn loop is converted to \texttt{\#pragma omp parallel for schedule(dynamic)} and the message counter uses \texttt{\#pragma omp atomic}. Build with \texttt{gcc-15 -fopenmp} or \texttt{clang -fopenmp}. pthreads still drive per-actor message loops (coexist with OpenMP) & OpenMP \\
  Drone / simulation & \texttt{drone\_simulator} & browser-abcl & obstacle-aware drone swarm with comm + view range & JS-B, JS-N \\
  Trace / minimal repros & \texttt{H}, \texttt{P}, \texttt{T*}, \texttt{LD*}, \texttt{MS}, \texttt{AA}, \texttt{PP}, \texttt{PH}, \texttt{line*}, \texttt{Philosophers5\_\{debug,trace\}} & abclc & reduced repro cases used during runtime / TLA+ / Spin model-checking & OCaml \\
  \bottomrule
\end{longtable}

\clearpage

\section*{Core language}

\begin{longtable}{F C C C C C C C}
  \toprule
  \textbf{Feature} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  method injection (\texttt{add\_method}/\texttt{remove\_method}) & \Yes & \Yes & \Yes & \Yes & \No & \No & \No \\
  \texttt{now} synchronous send                                   & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes \\
  \texttt{future} async send                                      & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes \\
  \texttt{await} future block                                     & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes \\
  \texttt{send} fire-and-forget                                   & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes \\
  \texttt{become} actor class swap                                & \Yes & \Yes & \Yes & \Yes & \No  & \No  & \No  \\
  \texttt{select} selective receive                               & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \No  \\
  top-level functions                                             & \Yes & \Yes & \Yes & \Yes & \No  & \No  & \Yes \\
  signatures / overloads                                          & \No  & \No  & \Yes & \Yes & \No  & \No  & \No  \\
  dynamic compile (\texttt{compile()})                            & \Yes & \Yes & \Yes & \Yes & \No & \No & \No \\
  worker pool / \texttt{DynamicWorkerPool}                        & \Yes & \Yes & \Yes & \Yes & \Part & \Part & \No \\
  text file I/O (\texttt{read\_file}/\texttt{write\_file}/\texttt{append\_file}/\texttt{file\_exists}) & \Yes & \Yes & \Yes & \Yes & \Yes\textsuperscript{a} & \Yes\textsuperscript{a} & \No \\
  image I/O (\texttt{image\_create}/\texttt{image\_load}/\texttt{image\_save}/\texttt{image\_size}/\texttt{image\_pixel}/\texttt{image\_set\_pixel}) & \Yes\textsuperscript{b} & \Yes\textsuperscript{b} & \Yes\textsuperscript{c} & \Yes\textsuperscript{c} & \Yes\textsuperscript{a\textsuperscript{,}c} & \Yes\textsuperscript{a\textsuperscript{,}c} & \No \\
  \bottomrule
\end{longtable}

\textsuperscript{a} \small JS-B / JS-N の I/O ビルトインは host が Node の \texttt{fs} を注入したとき (\texttt{server.mjs} が自動で行う) のみ動作。ブラウザ内では "not available" を投げる。\\
\textsuperscript{b} \small Py-A / Py-I は Pillow 経由で PNG / JPEG / GIF / WebP を判定して読み書き。\\
\textsuperscript{c} \small OCaml / JS-O / JS-B / JS-N は純ランタイムの PPM (P6) バックエンド (RGBA メモリ表現、保存時はアルファ破棄)。PNG は外部ライブラリが必要。

\section*{Type system}

\begin{longtable}{F C C C C C C C}
  \toprule
  \textbf{Feature} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  type inference                  & \Part trace & \Yes HM & \Yes HM & \Yes HM & \Yes flow & \Yes flow & \Yes HM+spec \\
  type annotations                & \Yes        & \Part ignored & \Yes & \Yes & \No & \No & \No \\
  records \texttt{\{a:int, b:str\}} & \Yes  & \Yes & \Yes & \Yes & \No & \No & \Part type only \\
  tuples \texttt{(1, "a")}          & \Yes  & \Yes & \Yes & \Yes  & \No  & \No  & \No  \\
  arrays (typed, multi-dim)         & \Yes  & \Yes & \Yes & \Yes & \Yes & \Yes & \Part \\
  generics on functions             & \No   & \Yes Forall & \Yes Forall & \Yes & \No & \No & \No \\
  \bottomrule
\end{longtable}

\section*{Phase 11+ advanced features}

Python (inferred) runs HM only, so the annotation-driven Phase 11+
static checks that Python (annotated) performs are not re-implemented.
Programs still execute at runtime via the shared interpreter, but
the static guarantees from Phase 12 / 14 / 15 / 16 are lost.
Phase 13 (channels), Phase 17 (\texttt{scope}), and the session-type /
protocol-trace / AIOS-registry families are runtime features, so
they remain available in Py-I.

\smallskip

\noindent
\textbf{Session types} here are \emph{runtime-checked} and explicitly
separate from HM inference: they observe values flowing over the
wire at runtime and validate them against a declared protocol spec
(\texttt{"actor.method(arg\_t1, ...) ! ret\_type -> ..."}).
Violations are recorded into \texttt{session\_events()} and counted;
the program does not abort.

\begin{longtable}{F C C C C C C C}
  \toprule
  \textbf{Feature} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  channels (CSP-style) — Phase 13          & \Yes & \Yes & \No & \No & \No & \No & \No \\
  linear types / use-after-move — Phase 14 & \Yes & \No  & \No & \No & \No & \No & \No \\
  owned / pub fields — Phase 15            & \Yes & \No  & \No & \No & \No & \No & \No \\
  effects \texttt{!\{fs,ai,net,mut\}} — Phase 12 & \Yes & \No  & \No & \No & \No & \No & \No \\
  structured concurrency \texttt{scope} — Phase 17 & \Yes & \Yes & \No & \No & \No & \No & \No \\
  transient cast at any-boundary — Phase 16 & \Yes & \No  & \No & \No & \No & \No & \No \\
  \textbf{session types} (runtime-checked typed protocols) & \Yes & \Yes & \No & \No & \No & \No & \No \\
  protocol traces (order-only)             & \Yes & \Yes & \No & \No & \No & \No & \No \\
  AIOS service registry (alias-resolved send) & \Yes & \Yes & \No & \No & \No & \No & \No \\
  \bottomrule
\end{longtable}

\clearpage

\section*{Phase C--E2 type inference (Python annotated only, added 2026-05-17/18)}

Phase 11--17 の checker layer に加え、Python (annotated) ランタイムは
\textbf{constraint-based Hindley--Milner + Z3 refinement} 推論系を
別モジュール \texttt{aipl\_inference.py} (\textasciitilde 1255 LOC) として持つ。
Py-I (\texttt{python-aipl-inferred/}) は依然 trace-based HM のままで,
これらの新 phase は取り込まれていない。

\begin{longtable}{l F C C C C C C C}
  \toprule
  \textbf{ID} & \textbf{Feature} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  CE-1 & Phase C: constraint-based HM + Z3 refinement (Int)      & \Yes & \No & \Part\textsuperscript{**} & \Part\textsuperscript{**} & \No & \No & \No \\
  CE-2 & Phase D-1: cross-class inference                        & \Yes & \No & \Yes\textsuperscript{\#} & \Yes\textsuperscript{\#} & \No & \No & \No \\
  CE-3 & Phase E-$\alpha$: \texttt{where} 句 in AIPL grammar      & \Yes & \No & \Part\textsuperscript{*} & \Part\textsuperscript{*} & \No & \No & \No \\
  CE-4 & Phase E-$\beta$: actor field 共有 (method 横断)         & \Yes & \No & \Yes\textsuperscript{\#} & \Yes\textsuperscript{\#} & \No & \No & \No \\
  CE-5 & Phase E-$\gamma$: record structural typing              & \Yes & \No & \Yes\textsuperscript{\#\#} & \Yes\textsuperscript{\#\#} & \No & \No & \No \\
  CE-6 & Phase E-$\gamma$-R: Real / Rat refinement (Z3 Real)     & \Yes & \No & \Part\textsuperscript{**} & \Part\textsuperscript{**} & \No & \No & \No \\
  CE-7 & Phase E-2: typeck $\times$ inference 統合 CLI \texttt{--check} & \Yes & \No & \Yes\textsuperscript{\$} & \Yes\textsuperscript{\$} & \No & \No & \No \\
  CE-8 & \texttt{--infer} standalone CLI (Phase D-4)             & \Yes & \No & \Yes\textsuperscript{\$} & \Yes\textsuperscript{\$} & \No & \No & \No \\
  CE-9 & refinement vacuously-false detection (declaration-time, Z3 unsat) & \Yes & \No & \Part\textsuperscript{**} & \Part\textsuperscript{**} & \No & \No & \No \\
  \bottomrule
\end{longtable}

\textbf{サンプル}: \texttt{aice-pi-evolution/experiments/2026-05-17\_aipl\_v2\_type\_inference/samples/feature\_\{a..g\}/}
(7 feature $\times$ 3 = 21 demo + 27/27 unit tests).

\smallskip
\noindent\textsuperscript{*} CE-3: OCaml は Phase O-2.a (2026-05-18) で
parsing 達成.\texttt{lexer.mll} に \texttt{where}/\texttt{and}/\texttt{or}/\texttt{not} キーワード,
\texttt{parser.mly} に \texttt{type\_expr WHERE refine\_or} 規則,
\texttt{ast.ml} に \texttt{TyERefined of type\_expr * refine\_pred} を追加.
サンプル: \texttt{abclc/WhereClause.abcl}.

\noindent\textsuperscript{**} CE-1 / CE-6 / CE-9: OCaml は Phase O-2.b
(2026-05-18) で SMT-LIB 2 + z3 CLI バックエンドを実装,
Phase O-2.c (同日) で Real 理論にも対応.
\texttt{src/refinement.ml} が \texttt{Ast.refine\_pred} を \texttt{smt\_sort}
(Int / Real) パラメータ付きで SMT-LIB 2 にレンダし \texttt{z3 -in -t:5000}
に流す.\texttt{AIPL\_REFINE\_CHECK=1} で typecheck pass が
vacuously-false な refinement に \texttt{[refine warning]} を吐く.
z3 不在時は \texttt{Deferred} で graceful fallback.
サンプル: \texttt{abclc/WhereVacuous.abcl} (Int), \texttt{abclc/WhereVacuousReal.abcl} (Real).

\smallskip
\noindent\textsuperscript{\#} CE-2 / CE-4: OCaml は Phase O-2.d (2026-05-18) で
cross-class 推論を強化.\texttt{preinfer\_all\_classes} が declared
return-type を尊重し,\texttt{Now} / \texttt{Future} expression が
\texttt{class\_method\_schemes} 越しに actual ret 型を返すよう変更.
\texttt{TypedVarDecl} の unify 失敗を Type\_error 化.
actor field の cross-method 一貫性 (Int/String mix 検出) も自動取得.

\smallskip
\noindent\textsuperscript{\#\#} CE-5: OCaml は Phase O-2.e (2026-05-18) で
record structural typing を完成.\texttt{Types.unify} の \texttt{TRecord}
ケースは既に label-sort + 再帰 unify を実装していたが,
\texttt{preinfer\_all\_classes} が method parameter annotation を
無視していたため cross-class 呼出しで shape 不一致が silent だった.
parameter annotation を尊重するよう修正後,\texttt{method f(p:\{a:int,b:int\})}
に shape 不一致の record literal を渡すと count / label / field-type
の全てで type error.サンプル: \texttt{abclc/RecordStructural.abcl}.

\smallskip
\noindent\textsuperscript{\$} CE-7 / CE-8: OCaml は Phase O-2.f
(2026-05-18) で CLI 統合.\texttt{repl\_thread.exe} に
\texttt{--type-check FILE} / \texttt{--infer FILE} / \texttt{--check FILE} / \texttt{--strict}
を追加.\texttt{--check} は Python-style の section banner 付き
出力,\texttt{--strict} で issue 時に exit 3.
Z3 refinement は \texttt{AIPL\_REFINE\_CHECK=1} を併用すると
\texttt{[refine warning] vacuously-false ...} が stderr に発火.

OCaml ランタイムも HM 推論を持つが (\#12 で \Yes),その他の refinement
機能 (CE-1, CE-6, CE-9) と record structural typing (CE-5) は今後の課題.

\clearpage

\section*{AIPL v2 Distributed runtime (\texttt{aipl\_dist}, added 2026-05-18)}

進化計算 (MAP-Elites 8 seed $\times$ 30 gen $\times$ 2 run) で発見された
分散ランタイム設計 (I0003 balanced / I0023 hang-resilience / I0036 Erlang
OTP の 3 候補) を \texttt{aipl\_dist.py} (591 LOC) として実装。AIPL 言語仕様
は不変,既存サンプルは無改変で同じ挙動。\textbf{\texttt{AIPL\_DIST\_ENABLE=1}
で全機能 opt-in}。

\begin{longtable}{l F C C C C C C C}
  \toprule
  \textbf{ID} & \textbf{Feature (env var)} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  DR-1 & I-1 \texttt{env\_var\_routing} (\texttt{AIPL\_ROUTE})       & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-2 & I-2 \texttt{structured\_log} (\texttt{AIPL\_DIST\_LOG\_FILE}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-3 & I-3 \texttt{token\_budget\_aware} (\texttt{AIPL\_DIST\_RPM} / \texttt{TPM}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-4 & I-4 \texttt{checkpoint\_and\_resume} (\texttt{AIPL\_DIST\_CHECKPOINT\_DIR}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-5 & IQ \texttt{quarantine\_and\_skip} (\texttt{AIPL\_DIST\_QUARANTINE\_TTL}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-6 & IM-1 \texttt{quorum\_replicate} (\texttt{AIPL\_DIST\_QUORUM\_PROVIDERS}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-7 & IM-2 \texttt{subtree\_quarantine} (\texttt{AIPL\_DIST\_SUBTREE\_QUARANTINE}) & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-8 & spawn-tree tracking (TLS-auto parent)                    & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  DR-9 & runtime hooks (interp / actor / call\_ai) — opt-in       & \Yes & \No & \Yes & \Yes & \No & \No & \No \\
  \bottomrule
\end{longtable}

\textbf{実装位置}: \texttt{src/python-aipl/aipl\_dist.py} (591 LOC, 新規) +
\texttt{aipl\_interp.py} (+27 行 spawn hook) +
\texttt{aipl\_runtime.py} (+13 行 error hook) +
\texttt{aipl\_ai.py} (+13 行 budget hook).

\textbf{サンプル}: \texttt{aice-pi-evolution/experiments/2026-05-17\_aipl\_v2\_type\_inference/IMPL\_I0003\_MVP/samples/}
(8 feature $\times$ 3 = 24 demo + 27/27 unit tests).

\textbf{設計探索の出自}: 同 dir の \texttt{AIPL\_v2\_Distributed.\{aice,ga.json,schema.json\}}
を MAP-Elites で 24 min $\times$ 2 run。詳細は \texttt{IMPL\_I0003\_MVP/IMPL\_DESIGN.md}
\textasciitilde{} \texttt{IMPL\_I0036\_REPORT.md} 参照。

\smallskip
\noindent OCaml は Phase O-1 + O-1.5 で全 9 機能 \Yes 達成.DR-3/DR-6 は
\texttt{Ai.call\_gemini} へ自動 wire 済,DR-8 は per-thread
\texttt{current\_actor} TLS で parent を自動取得.

\clearpage

\section*{Networking, AI, infrastructure}

\begin{longtable}{F C C C C C C C}
  \toprule
  \textbf{Feature} & \textbf{Py-A} & \textbf{Py-I} & \textbf{OCaml} & \textbf{JS-O} & \textbf{JS-B} & \textbf{JS-N} & \textbf{C} \\
  \midrule
  \endhead

  remote actors (HTTP)                          & \Yes & \Yes & \Yes\textsuperscript{d} & \Yes\textsuperscript{d} & \No & \Part srv only & \Yes via OCaml \\
  WebSocket                                     & \Yes \texttt{ws\_listen/send/close} builtins (websockets lib) & \Yes (=Py-A) & \Yes built-in (verified 101) & \Yes via OCaml & \Yes browser-native & \Yes \texttt{/ws?sid=} + \texttt{/api/broadcast} (ws lib) & \Yes \texttt{abcl\_ws\_runtime.c} + libwebsockets \\
  AI integration (\texttt{ai\_call})            & \Yes stream/img & \Yes & \Yes & \Yes & \Yes mock & \Yes mock & \Yes \\
  \quad provider arg (\texttt{ai\_call([1|2|3,] prompt)}) — 1=gemini, 2=anthropic, 3=openai & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes (via OCaml) \\
  \quad \texttt{now actor.m(...)} + \texttt{ai\_call} inside method (blocking) & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \No \\
  \quad \texttt{future actor.m(...)} + \texttt{await(f)} + \texttt{ai\_call} & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \No \\
  \quad \texttt{send}\,+\,callback pattern with \texttt{ai\_call} & \Yes & \Yes & \Yes & \Yes & \Yes & \Yes & \No \\
  AI governance (budget/concurrent/fallback)    & \Yes & \Yes & \Yes & \Yes & \No & \No & \Yes \\
  HMAC-signed remote send                       & \Yes & \Yes & \Yes\textsuperscript{e} & \Yes\textsuperscript{e} & \No & \No & \Yes \\
  persistent actor state                        & \Yes & \Yes & \No & \No & \No & \No & \No \\
  live dashboard (SSE)                          & \Yes & \Yes & \Part polling & \Part & \No & \No & \Yes \\
  \texttt{/api/typecheck} JSON endpoint         & \No & \No & \Yes & \Yes & \No & \Yes & \NA \\
  \texttt{/api/run} JSON endpoint               & \No & \No & \No & \No & \No & \Yes & \NA \\
  \bottomrule
\end{longtable}

\section*{Actor concurrency model}

Every AIPL implementation runs each actor as a separate
concurrent unit, but the kind of unit differs by 3--4 orders of
magnitude in weight.  This affects how many actors a program can
usefully spawn, how messages interleave, and whether blocking
I/O in one actor stalls the whole runtime.

\begin{longtable}{p{4.0cm} p{4.0cm} p{6.0cm} p{4.0cm}}
  \toprule
  \textbf{Runtime / codegen target} & \textbf{Model} & \textbf{Per-actor unit} & \textbf{Source pointer} \\
  \midrule
  \endhead

  Python (annotated)        & \textbf{1:1 OS thread} & \texttt{threading.Thread(daemon=True)} & \texttt{aipl\_runtime.py:92} \\
  Python (inferred)         & \textbf{1:1 OS thread} & (inherits from python-aipl) & (re-uses interp) \\
  OCaml                     & \textbf{1:1 OS thread} & \texttt{Thread.create} & \texttt{eval\_thread.ml:\textasciitilde 1168} \\
  JS-OCaml (server)         & \textbf{1:1 OS thread} & (= OCaml backend threads) & (= OCaml) \\
  JS-Browser (browser-abcl) & \textbf{cooperative event loop} & \texttt{setTimeout}-driven mailbox drain & \texttt{runtime.js:\textasciitilde 233} \\
  JS-Node (node-aipl-server)& \textbf{cooperative event loop} & (= browser-abcl runtime) & re-export \\
  C (default)               & \textbf{1:1 OS thread} & \texttt{pthread\_create} & \texttt{c\_translator.ml:\textasciitilde 721,790} \\
  C + SDL2                  & \textbf{1:1 OS thread} & \texttt{pthread\_create} & (same as default C) \\
  C + Xinu                  & \textbf{1:1 OS process} (kernel-level) & Xinu \texttt{create()} + \texttt{ready()} & \texttt{c\_translator.ml:\textasciitilde 1077} \\
  C $\to$ Python codegen    & \textbf{1:1 OS thread} & \texttt{threading.Thread} (\texttt{o.\_spawn()}) & \texttt{c\_translator.ml:\textasciitilde 1847} \\
  C $\to$ Pony codegen      & \textbf{M:N lightweight} (runtime-scheduled) & Pony actor (work-stealing) & implicit in generated \texttt{actor} \\
  C $\to$ Erlang codegen    & \textbf{M:N lightweight} (BEAM) & BEAM process (\texttt{spawn(fun()->...end)}) & \texttt{c\_translator.ml:\textasciitilde 2413} \\
  C $\to$ Go codegen        & \textbf{M:N lightweight} (Go runtime) & goroutine (\texttt{go c.run()}) & \texttt{c\_translator.ml:\textasciitilde 2728} \\
  C $\to$ Prolog codegen    & \textbf{1:1 OS thread} & SWI thread (\texttt{thread\_create/3}) & \texttt{c\_translator.ml:\textasciitilde 3060} \\
  \bottomrule
\end{longtable}

\subsection*{Three concurrency tiers}

\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item \textbf{1:1 OS thread / process} (Python, OCaml, C, C-SDL2,
    Xinu, C$\to$Python, C$\to$Prolog): each actor is a kernel-scheduled thread.
    Simple mental model; blocking I/O in one actor doesn't stall
    others.  Scales to $\sim$100--1000 actors before kernel
    overhead dominates.
  \item \textbf{M:N lightweight} (Pony, Erlang, Go): actors are
    scheduled by the language runtime onto a small pool of OS
    threads.  Scales to $\sim 10^{5}$--$10^{7}$ actors.
    Blocking syscalls in one actor may stall its scheduler unless
    the runtime intercepts them (Erlang's BEAM intercepts; Go's
    runtime intercepts via \texttt{netpoll}; Pony's runtime
    requires non-blocking idioms).
  \item \textbf{Cooperative event loop} (browser-abcl,
    node-aipl-server): single OS thread; mailboxes are drained
    turn-by-turn through \texttt{setTimeout(0)} re-entry into the
    event loop.  No real parallelism, but message-level
    interleaving is preserved.  A long synchronous computation in
    one actor \emph{will} stall all others --- by design, since
    this is the only concurrency model the browser DOM allows.
\end{itemize}

\subsection*{Cross-cutting implication}

The AIPL programming model (\texttt{send} / \texttt{now} /
\texttt{future} / \texttt{await}) is preserved across all three
tiers --- the choice of target picks a point on the cost/scale
curve without changing the program text.  The same \texttt{.abcl}
file:
\begin{itemize}[leftmargin=*,itemsep=0pt]
  \item runs $\sim$1000 actors fine on Python / OCaml / C
    (OS-thread tier);
  \item scales to millions of actors on Erlang / Go / Pony codegen;
  \item runs in a browser sandbox via JS-Browser at the cost of
    single-threaded execution.
\end{itemize}

\clearpage

\section*{C-version-specific codegen targets}

\begin{tabularx}{\linewidth}{X C}
  \toprule
  \textbf{Target} & \textbf{C} \\
  \midrule
  C + pthread standalone binary              & \Yes \\
  C + SDL2 GUI binary (1178-line runtime)    & \Yes \\
  Xinu embedded-OS target (\texttt{--xinu})  & \Yes \\
  Python target (\texttt{--python})          & \Yes \\
  \textbf{Pony target (\texttt{--pony})} — \texttt{class}$\to$\texttt{actor}, methods$\to$\texttt{be}, two-step \texttt{\_aipl\_init} & \Yes \\
  \textbf{Erlang target (\texttt{--erlang})} — \texttt{class}$\to$\texttt{spawn}+\texttt{receive} loop, fields$\to$versioned loop args & \Yes \\
  \textbf{Go target (\texttt{--go})} — \texttt{class}$\to$struct+goroutine, methods$\to$typed message structs + type-switch dispatch & \Yes \\
  \textbf{Prolog target (\texttt{--prolog})} — \texttt{class}$\to$SWI thread + receive loop, expressions hoisted to goals (\texttt{is/2}, \texttt{format(atom(...))}) & \Yes \\
  \bottomrule
\end{tabularx}

\clearpage

\section*{Key observations}

\subsection*{Python (annotated) is the most feature-complete runtime}

Of the 30 surveyed features, Python (annotated) ticks roughly 28.
It owns the full Phase 11--17 stack (channels, linear types, owned
fields, effects, structured concurrency, transient casts), method
injection, dynamic compile, persistent actor state, full AI
integration (streaming + multimodal images), and Pillow-backed
image I/O (PNG / JPEG / GIF / WebP).

\subsection*{Python (inferred) --- Python's HM-typed twin}

Runtime features match Python (annotated) exactly because the
interpreter is shared.  The trade is on the static side: the Phase
11+ annotation-driven checks are replaced with a Hindley-Milner
inference pass.  Inferred types cross-verified against OCaml: on
shared samples (\texttt{Hello.abcl}, \texttt{counter.abcl}) the two
runtimes produce identical type schemes.

\subsection*{OCaml --- feature parity with Python on the core}

After the OCaml feature catch-up, the runtime supports
\textbf{method injection} (\texttt{add\_method}/\texttt{remove\_method}),
\textbf{dynamic compile} (\texttt{compile()} + \texttt{spawn()}),
\textbf{top-level functions} with \textbf{generics}
(\texttt{function id(x: T) -> T}), \textbf{type annotations},
\textbf{records \& tuples}, \textbf{sized arrays}
(\texttt{var x[N][M]}), and \textbf{text/image file I/O} (PPM
backend for images).  Phase 11+ effect / linear / owned / transient
features still only exist as \texttt{.abcl} design-document samples
\textemdash{} not statically enforced.  Solid \texttt{become} /
\texttt{select} / WebSocket (built into \texttt{web\_gateway.ml})
and HTTP remote actor calls (\texttt{send}\,/\,\texttt{now}\,/\,\texttt{future},
error-tolerant) with \textbf{HMAC-signed traffic}
(\texttt{ABCL\_REMOTE\_SECRET} 環境変数, 純 OCaml HMAC-SHA256 in
\texttt{src/hmac\_sha256.ml}, Python / C 版とワイヤ互換).

\textsuperscript{d} \small OCaml の remote client (\texttt{src/remote\_client.ml}) は \texttt{send} (fire-and-forget) / \texttt{now} (同期 reply) / \texttt{future} (並列 + await) の 3 形式すべてをサポート。ECONNREFUSED / timeout は stderr に記録され、呼び出し側のアクターは生存する。エンドツーエンド検証は \texttt{src/test\_remote\_actor.sh} (9/9)。JS-O は OCaml バックエンド経由でこれを継承する。

\textsuperscript{e} \small 純 OCaml HMAC-SHA256 (\texttt{src/hmac\_sha256.ml}, RFC 6234 / 4231 テストベクタで verify) を sender と receiver の両側で使う。\texttt{ABCL\_REMOTE\_SECRET} が両端でセットされたとき、全リクエストに \texttt{X-ABCL-Sig: <hex>} が付与される。\texttt{web\_gateway.ml} の \texttt{verify\_hmac\_or\_reject} は不一致なら 401 を返す。\texttt{python-aipl/aipl\_remote.py} と C ランタイムにワイヤ互換。以前は受信側が \texttt{openssl dgst} をサブプロセス起動していたが、インプロセスの純 OCaml 版に置換 (リクエスト毎の fork 排除)。

\subsection*{JS-OCaml $\approx$ OCaml}

A thin HTTP client (\texttt{src/app.js} etc.) over the OCaml
backend exposed by \texttt{web\_gateway.ml}.  Every OCaml-side
feature \textemdash{} method injection, dynamic compile, top-level
functions, generics, type annotations, file \& image I/O
\textemdash{} is reachable via \texttt{/api/repl} from a browser or
\texttt{curl}.  The OCaml-specific differentiator is the
\texttt{/api/typecheck} JSON endpoint.

\subsection*{JS-Browser --- minimal in-browser engine}

Basic actor model plus flow-sensitive type inference.
Recently picked up \textbf{sized arrays} (\texttt{var x[N]}) and
\textbf{text \& image file I/O} (host-injected \texttt{fs}; throws
in browsers).  No \texttt{become}, no channels, no remote, no
method injection.  AI integration exists but is mock-only.

\subsection*{JS-Node $\approx$ JS-Browser + fs}

Wraps the browser-abcl runtime inside a Node.js HTTP server
(\texttt{src/node-aipl-server/server.mjs}); auto-injects Node's
\texttt{fs} so the text \& image I/O builtins are functional.
Exposes \texttt{/api/typecheck} and \texttt{/api/run} for headless
use.  Same flow-sensitive type inference as browser-abcl.

\subsection*{C codegen --- broadest target diversity}

\texttt{become} and \texttt{select} remain codegen-unsupported
(emit \texttt{/* unsupported */}), but the codegen inherits OCaml's
HMAC, SSE, and AI governance, and offers \textbf{9 backends} from
one front-end:
\begin{itemize}\itemsep0pt
  \item C + pthread (default)
  \item \textbf{C + LLVM / clang} (\texttt{--llvm}: adds
    \texttt{\_\_attribute\_\_((hot/cold))} + headers for
    \texttt{clang -emit-llvm})
  \item \textbf{C + OpenMP} (\texttt{--openmp}: \texttt{\#pragma omp
    parallel for} spawn loop + \texttt{\#pragma omp atomic} counter)
  \item C + SDL2 (GUI demos)
  \item Xinu / Python / Pony / Erlang / Go / SWI-Prolog
\end{itemize}
Fields, parameters, and locals are specialised to native C types
via HM inference + per-class field-type registry.

\section*{Method injection \textemdash{} now in three runtime families}

Both Python variants and the OCaml runtime (with JS-OCaml piggy-backing
on it) implement mutable method dispatch tables with runtime
\texttt{add\_method} / \texttt{remove\_method} / \texttt{methods\_of}.
\texttt{browser-abcl} and \texttt{node-aipl-server} still lack even
\texttt{become} and rely on the unchanging method table baked at
parse time.

\section*{Type inference cross-verification}

For shared samples (\texttt{abclc/Hello.abcl},
\texttt{abclc/counter.abcl}), the three HM-based runtimes
(\textbf{OCaml}, \textbf{Python-inferred}, \textbf{C}) produce
identical inferred types:

\begin{verbatim}
Hello:   count : float
         init  : (int) -> unit
         greet : () -> unit
         inc   : () -> unit

Counter: count : float
         inc   : () -> unit
         dec   : (float) -> unit    <- monomorphized via call site
\end{verbatim}

The flow-sensitive variants (\textbf{JS-Browser},
\textbf{JS-Node}) reach the same answers on shared samples within
the limits of their algorithm — field types match, but method
signature schemes are not produced the same way.

\section*{Phase 11+ implementation gap}

The full Phase 11--17 stack is implemented only in Python
(annotated).  This represents AIPL's language-evolution frontier;
Python (annotated) serves as the research runtime.  Python
(inferred) keeps the runtime side but trades the static checks for
HM-style polymorphic inference --- a different research axis
(declarative typing vs. annotated capabilities).

\bigskip
\hrule
\smallskip

\noindent\small
\subsubsection*{Post-Phase-11 (added 2026-05-17/18)}

\begin{center}
\begin{tabular}{lll l}
\toprule
領域 & Py-A 達成 & 他ランタイム & 関連 ID \\
\midrule
Phase C--E2 型推論 (HM + Z3 refinement) & 9/9 & 0/9 & CE-1 $\sim$ CE-9 \\
AIPL v2 Distributed (\texttt{aipl\_dist}) & 9/9 & \textbf{OCaml 9/9} (= JS-O) & DR-1 $\sim$ DR-9 \\
\bottomrule
\end{tabular}
\end{center}

\textbf{Py-A 単独で進化計算由来の 18 機能が opt-in 利用可能}
(\texttt{AIPL\_DIST\_ENABLE=1} または \texttt{--check} / \texttt{--infer}
で発火,デフォルトは従来挙動と同一)。OCaml 側への移植 path:

\begin{itemize}\setlength{\itemsep}{0pt}
\item 型推論側: \texttt{--infer} の HM-with-refinement を OCaml HM
  (\texttt{src/infer.ml}) に統合すれば CE-1, CE-2, CE-4 は取り込み可能.
  \texttt{where} 句 (CE-3) は OCaml lexer/parser 修正が必要.Z3 backend
  (CE-1, CE-6, CE-9) は別プロセス via JSON が現実的.
\item ランタイム側: DR-1 / DR-2 / DR-4 / DR-5 / DR-8 / DR-9 は OCaml
  \texttt{eval\_thread.ml} + \texttt{aipl\_remote.ml} への hook 追加
  (\textasciitilde{}200 LOC) で実装可能.DR-3 / DR-6 は AI client 層,
  DR-7 は spawn-tree registry が要る.
\end{itemize}

\bigskip

\noindent
\textit{Generated 2026-05-14, updated 2026-05-18.}
Cumulative through: OCaml feature catch-up (records / tuples / dynamic
compile / method injection / top-level functions / generics / type
annotations / sized arrays / text \& image I/O);
C-codegen LLVM / OpenMP targets;
\texttt{python-aipl-inferred} (commit \texttt{270f291}) and
\texttt{node-aipl-server} (commit \texttt{adeb0b6});
\textbf{Phase C--E2 constraint-based HM + Z3 refinement}
(\texttt{aipl\_inference.py}, 1255 LOC; sections CE-1..CE-9);
\textbf{AIPL v2 Distributed runtime} (\texttt{aipl\_dist.py}, 591 LOC;
sections DR-1..DR-9), discovered by MAP-Elites GA + MVP-implemented for
I0003 / I0023 / I0036 winners.

For source pointers, run \texttt{grep} against the files listed in
each runtime's source column.

\end{document}
