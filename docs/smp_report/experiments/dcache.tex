% ======================================================================
% experiments/dcache.tex — Level 2: enabling the D-cache
%
% ★ 軸はボードではなく「キャッシュ構成」。ゆえに boards/ ではなく
%    experiments/ に置く。同じ枠組みで別の軸を切りたくなったら
%    experiments/ に足す（例: 動的ワークスティーリング = Level 3）。
%
%    設計・制約・DMA棚卸し・ハーネスは測定に依存せず確定。
%    D-cache ON の「スケールするか」の実測値だけがシリアル取得待ち
%    （§末尾の \TODO を、数値が出たら差し替える）。
% ======================================================================

\subsection{動機：支配変数はキャッシュ構成である}

第 \ref{sec:rpi3} 節の測定で最も効いていた制約は、SMP の方式ではなく
\textbf{D-cache が OFF であること}であった。メモリ律速がスケールしなかったのも
（\bench{fill} \spd{0.93}）、\texttt{volatile} を外したら \spd{3.04} が
\spd{3.66} に上がったのも（第 \ref{sec:rpi3-volatile} 項）、動的ワークスティーリングを
断念して静的分割にしたのも、すべて D-cache OFF が原因である。

そこで問いは：\textbf{D-cache を ON にすると、メモリ律速の処理はスケールし始めるか}。
もしそうなら「ウィンドウシステムは並列化しない」という第 \ref{sec:rpi3}\,節の
結論が覆る。この問いは SMP の方式とは独立であり、キャッシュ構成という別の軸に属する。

\subsection{D-cache ON は「フラグを 1 にする」ではない}

素朴に \texttt{SCTLR.C} を 1 にするのは\textbf{危険で、過去に一度 Pi 3 を
brick させている}（\texttt{mmu.c} 冒頭に記録あり）。安全に ON にするには、
この移植に 1 行も存在しなかったキャッシュ管理を新規に書く必要があった。
本実験で顕在化した罠は 4 つある。

\begin{enumerate}[leftmargin=1.6em]
  \item \textbf{リセット直後のキャッシュはゼロでなくゴミ。}
        invalidate せずに \texttt{C=1} にすると、コアはゴミを有効データと
        見なす。これが brick の原因である。set/way invalidate ループを
        \texttt{C=1} の\textbf{前に}実行しなければならない（後では、いま
        キャッシュした本物のデータを捨ててしまう）。

  \item \textbf{Cortex-A53 は SMPEN なしでは非コヒーレント。}
        \texttt{mmu.c} は RAM を shareable にマップしているが、
        A53 では shareable かつ cacheable なメモリでも \texttt{CPUECTLR.SMPEN}
        を立てるまで\textbf{コア間でコヒーレントにならない}。\texttt{C=1} かつ
        SMPEN=0 だと、第 \ref{sec:design} 節のロックフリー・ジョブメールボックス
        （\texttt{volatile} + \texttt{dsb}）が静かに壊れる。\texttt{volatile} は
        コンパイラを縛るだけで、キャッシュは縛らない。

  \item \textbf{二次コアの起動窓。}
        二次コアは \texttt{\_smp\_start} の時点で\textbf{まだ MMU も
        キャッシュも OFF}であり、その状態でスタックポインタを RAM から読む。
        この読みはコア 0 のキャッシュをスヌープしない。ゆえにコア 0 は
        スタックアドレスを\textbf{起こす前に clean}せねばならず、さもなくば
        二次コアは null スタックで起きて最初の push で死ぬ。

  \item \textbf{\texttt{mmu\_disable} の前提が崩れる。}
        「キャッシュ OFF だから flush 不要」というコメントが \texttt{C=1} で
        偽になる。\texttt{kexec} 前にキャッシュを落とすと、\texttt{/upload} が
        キャッシュ経由で書いた次カーネルのイメージが dirty ライン内に取り残され、
        \textbf{stale メモリへジャンプ}する。clean を追加した。
\end{enumerate}

\noindent
これらを \texttt{cache.c}（set/way・範囲・SMPEN の各操作を新規実装）と、
\texttt{mmu.c} の \texttt{DCACHE\_ON} 経路（順序が命：invalidate と SMPEN を
\texttt{C=1} の前に）で処理した。

\subsection{最大の障害：USB DMA はページ属性で直せない}
\label{sec:dcache-usb}

D-cache ON でも DMA するハードとの共有メモリは非コヒーレントになる。
そこでカーネル全体の\textbf{DMA 地点を棚卸し}した。結果は明快で、
FB・電源・WiFi のメールボックスや SD/SDIO は\textbf{全て PIO}（DATA レジスタ
経由）であり、コヒーレンシ問題を持たない。唯一の DMA エンジンは
\textbf{DWC USB}であり、これが有線 LAN を担っている。

そして USB こそが\textbf{構造的に直せない}地点であった。DWC ドライバは
呼び出し側のポインタをそのまま DMA アドレスに渡し、呼び出し側は
\textbf{ヒープ}バッファ（\texttt{memget} は 8 バイト境界＝隣の割り当てと
64 バイトのキャッシュラインを共有）と、あろうことか\textbf{スタック上の
局所変数}（\texttt{smsc9512.c} のレジスタ操作が 4 バイトを DMA する）を渡す。
64 バイトのラインを invalidate すれば、\textbf{同じラインにある戻り番地や
退避レジスタごと破棄}する。これはページ属性では解けず、バウンスバッファと
ドライバ全域の 64 バイト境界化を要する。

この発見自体が結論である：\textbf{D-cache を入れられないのはキャッシュ管理を
書くのが面倒だからではなく、ドライバの DMA バッファの出自が構造的に許さない
から}である。

\subsection{実験の切り分け：USB を解かずに問いに答える}

問いは「D-cache でメモリ律速はスケールするか」であり、これに答えるのに
USB を解く必要はない。そこで実験用ビルド変種を用意した。

\begin{center}
\texttt{-DDCACHE\_EXPERIMENT -DDCACHE\_ON -DOS\_MINIMAL}
\end{center}

\noindent
\texttt{OS\_MINIMAL} でネットと WM を落とし、\texttt{DCACHE\_EXPERIMENT} で
さらに \texttt{usbinit}（唯一の DMA エンジン）を落とす。これで DMA する相手が
一つも存在しない状態で \texttt{C=1} にでき、USB の構造問題を回避したまま
本題を測れる。ただし\textbf{HDMI は残した}——ユーザ要求であり、また
フレームバッファの扱いが D-cache ON 実装の良い試金石でもあるためである。

HDMI を残すには 2 つの手当てが要った。FB のメールボックス要求構造体は
元は\textbf{スタック上}にあり（GPU が stale RAM を読み \texttt{address=0}
＝SYSERR ＝画面出ず）、64 バイト境界・64 バイト詰めの専用 static に移して
handshake の前後で clean/invalidate した。FB のピクセル領域自体は、
GPU がアドレスを返した\textbf{後に} \texttt{mmu\_set\_range\_noncached()} で
Normal Non-cacheable へ貼り替えた（アドレスは \texttt{mmu\_init} の後に
判明するため、静的な表には焼けない）。

\subsection{測定ハーネス}

実験変種はネットが無いので \route{/bench} を提供できない。そこでワークロード本体を
\texttt{apps/smpbench.c} に切り出し、\textbf{HTTP 経路（既定カーネル）と起動時
コンソール経路（実験カーネル）が同一のコードを呼ぶ}ようにした。キャッシュ
ON/OFF の比較は、両者が同一実装を走らせて初めて意味を持つ——別実装なら
「キャッシュ設定の差」ではなく「実装の差」を測ってしまう。この切り出しの
過程で、第 \ref{sec:rpi3-measbug} 項の測定バグを発見・修正している。

実験カーネルは起動時に \texttt{smpbench\_run\_all()} を実行し、\bench{fill} の
サイズ掃引を中心とした結果をシリアルへ出力する。

\subsection{実測結果}
\label{sec:dcache-result}

D-cache ON カーネルを実機に投入し、起動時ハーネスの出力をシリアルで取得した。
まず 3 つの前提が\textbf{すべて確認された}。

\begin{itemize}[leftmargin=1.6em]
  \item \textbf{起動した（brick 回避）}。Xinu のバナーからシェルまで正常に立ち
        上がった。set/way invalidate を \texttt{C=1} の前に置く順序が効いている。
  \item \textbf{\texttt{cores\_online = 4}、全ベンチ \texttt{agree = ok}}。
        SMPEN を立てたことで、\texttt{C=1} 下でもロックフリー・ジョブメールボックスの
        コヒーレンシが保たれている。逐次と並列の答えは全ベンチで一致した。
  \item \textbf{\texttt{sctlr = 0x00c5183d}}。D-cache OFF の \texttt{0x00c51839} に
        対しビット 2（C）が立ち、実機で D-cache が有効化されたことを裏付ける。
\end{itemize}

\subsubsection*{本命：メモリ律速はキャッシュでスケールする——ただし容量まで}

\begin{table}[htbp]
\centering
\caption{\bench{fill} の速度向上：D-cache OFF vs ON（8 パス、作業セット掃引）}
\label{tab:dcache-fill}
\small
\begin{tabular}{@{}rccl@{}}
\toprule
語数（作業セット） & OFF の速度向上 & ON の速度向上 & \\
\midrule
64          & \spd{0.38} & \spd{2.00} & \\
256         & \spd{0.61} & \spd{2.50} & \\
1{,}024 (4\,KB)   & \spd{1.13} & \spd{3.60} & \\
4{,}096 (16\,KB)  & \spd{1.27} & \spd{3.88} & \\
16{,}384 (64\,KB) & \spd{1.34} & \spd{4.01} & ← 完全スケール \\
65{,}536 (256\,KB)& \spd{1.24} & \spd{2.51} & ← 低下開始 \\
262{,}144 (1\,MB) & \spd{1.35} & \spd{1.40} & ← 帯域律速へ回帰 \\
\bottomrule
\end{tabular}
\end{table}

\noindent
表 \ref{tab:dcache-fill} が本実験の答えである。第 \ref{sec:rpi3} 節の結論
「メモリ律速はスケールしない」は、\textbf{D-cache OFF に固有の現象}であった。
D-cache ON では、作業セットがキャッシュに収まる限り\textbf{メモリ書込は
理想的にスケールする}（64\,KB で \spd{4.01}）。

しかしスケールするのは容量までである。作業セットが 256\,KB を超えると速度向上は
崩れ、1\,MB では \spd{1.40} まで戻る。これは\textbf{キャッシュ容量効果}である。
小さい \bench{fill} は同じ領域を 8 回書くため 2 回目以降はキャッシュに当たり、
実質\textbf{計算律速}になってスケールする。作業セットがキャッシュ（Pi 3 の
Cortex-A53 は L1 データ 32\,KB／コア＋共有 L2 512\,KB）を超えると、
書き戻しと充填で\textbf{再び帯域律速}に戻る。速度向上が 64\,KB で頂点、
256\,KB で崩れ始めるのは、この容量境界と整合する。

\subsubsection*{ウィンドウシステムの結論は覆るか——覆らない}

一見、これは「WM は並列化しない」（第 \ref{sec:rpi3} 節）を覆すように見える。
だが\textbf{覆らない}。理由が 2 つある。

第一に、\textbf{実フレームバッファはキャッシュ不可にマップしてある}
（第 \ref{sec:dcache-usb} 項、\texttt{mmu\_set\_range\_noncached}）。GPU が
スヌープしないため、これは D-cache ON でも変えられない。表 \ref{tab:dcache-fill}
の \bench{fill} は\textbf{通常のキャッシュ可能バッファ}での測定であり、
実描画はこの恩恵を受けない。

第二に、恩恵を受けるのは作業セットがキャッシュに収まる場合だけだが、
全画面ブリットは 1280$\times$800$\times$4 = 3.9\,MB でキャッシュを大きく超え、
仮にキャッシュ可能でも \spd{1.40} 程度に留まる。

したがって結論は次のように\textbf{精緻化}される：メモリ律速の処理は、
D-cache ON かつ\textbf{作業セットがキャッシュ内}なら 4 コアでスケールする。
だが実描画は（キャッシュ不可の FB へ、キャッシュを超える量を書くため）
どちらの条件も満たさず、依然スケールしない。中間バッファ上での
合成のように、キャッシュ内で完結する処理を挟めば話は別である。

\subsubsection*{副産物：D-cache は単一コア性能も大きく上げる}

計算律速側では、\bench{dining}(\spd{3.99})・\bench{primes}(\spd{2.05}) の
速度向上比は D-cache OFF とほぼ不変であった（元々スケールしていたため）。
一方で\textbf{絶対性能}は激変した。\bench{nqueens n=12} の 1 コア時間は
D-cache OFF の約 167{,}000\,\textmu s から\textbf{約 19{,}000\,\textmu s へ、
実に 8.8 倍高速化}している。クロック差（起動直後 2.33 倍）だけでは説明できず、
再帰探索が触る盤面配列 \texttt{cols[]} とスタックがキャッシュに載った効果である。
並列スケーリングとは独立に、D-cache は\textbf{計算律速の絶対性能そのもの}を
底上げする。

\subsubsection*{ただし本番投入はできない}

以上は\textbf{実験変種}（\texttt{DCACHE\_EXPERIMENT}、USB を落とした構成）での
結果である。本番カーネルに D-cache を入れるには、第 \ref{sec:dcache-usb} 項の
USB DMA 問題——ヒープとスタックのバッファへのバウンス、およびドライバ全域の
64 バイト境界化——を解く必要がある。それは本実験の範囲外であり、次段階の課題である。

\subsection{付録：実機シリアル出力（D-cache ON）}

参考のため、実機（コールドブート）から取得した起動時ハーネスの生出力を示す。

\begin{lstlisting}[language=,basicstyle=\ttfamily\scriptsize]
===== smpbench =====
cores_online = 4
D-cache      = ON   (SCTLR.C=1, SMPEN set)
sctlr        = 0x00c5183d

-- compute-bound (expect x3.2-x4.0) --
  bench           1-core us  N-core us  speedup
  dining n=5          51290      12828   x3.99  ok
  nqueens n=11         3716       1027   x3.61  ok
  nqueens n=12        19024       4865   x3.91  ok
  primes 200k         66761      32550   x2.05  ok

-- memory-store-bound: THE QUESTION (x1.0 = no benefit) --
  words           1-core us  N-core us  speedup
  fill 64                 2          1   x2.00  ok
  fill 256                5          2   x2.50  ok
  fill 1024              18          5   x3.60  ok
  fill 4096              70         18   x3.88  ok
  fill 16384            285         71   x4.01  ok
  fill 65536           1177        468   x2.51  ok
  fill 262144          4682       3330   x1.40  ok

-- pool round trip (the parallelise/don't break-even) --
  null                    0          1   x0.00  ok

===== end smpbench =====
\end{lstlisting}
\subsection{本番化の試みと限界：USB DMA は越えられなかった}
\label{sec:dcache-prod}

第 \ref{sec:dcache-usb} 項で「USB は構造的に難しい」と述べたが、実際に
本番化を試みた。結果は\textbf{部分成功}であり、その失敗の様相そのものが
価値ある結果なので記録する。

\subsubsection*{方式：全 USB 転送を非キャッシュ・バウンス経由に}

USB DMA の障害は、バッファがヒープ（\texttt{memget} は 8 バイト境界＝
64 バイトのキャッシュラインを隣と共有）やスタック上にあり、per-transfer の
clean/invalidate が\textbf{隣接データを巻き込む}ことだった。そこで
per-transfer 管理を避け、\textbf{全転送を専用の非キャッシュ・アリーナ経由に
バウンス}する方式を採った。DWC ドライバは元々、非境界バッファ用のバウンス
経路（\texttt{aligned\_bufs} + TX/RX コピー）を持つので、その切替を
「D-cache ON なら常にバウンス」に変え、アリーナを非キャッシュにマップした。
非キャッシュ領域は CPU も DMA も同一 RAM を直接見るので、原理上キャッシュ
管理命令が一切不要になる。

\subsubsection*{実機結果：起動と HDMI は成功、USB は全滅}

\begin{table}[htbp]
\centering
\caption{D-cache 本番化（フルカーネル）の実機結果}
\small
\begin{tabular}{@{}ll@{}}
\toprule
項目 & 結果 \\
\midrule
D-cache ON で起動 & $\checkmark$ brick なし（デスクトップ表示） \\
HDMI / フレームバッファ & $\checkmark$ 動作（FB の非キャッシュ再マップが有効） \\
set/way invalidate + SMPEN & $\checkmark$ 機能 \\
単一コア性能 & $\checkmark$ 8.8 倍（実験変種で実測） \\
\textbf{USB（マウス・キーボード・ethernet）} & $\times$ 全滅 \\
\bottomrule
\end{tabular}
\end{table}

\noindent
HDMI が正しく映ることは、非キャッシュ化の機構そのもの
（\texttt{mmu\_set\_range\_noncached}）が\textbf{正しく動作している証拠}である
（フレームバッファも同じ関数で非キャッシュ化しており、壊れていれば画面が
乱れる）。にもかかわらず USB は全滅した。Raspberry Pi 3 では
マウス・キーボード・ethernet がすべて LAN9514 ハブの背後にあるため、
USB が一つ壊れると全滅する。実機シリアルログには
\texttt{[webactor] autostart: ETH0 never came up} が記録された。

\subsubsection*{四つの修正を試みた——いずれも解決せず}

原因を、実機を最小限しか使わずオフラインのコードレビューで追った。

\begin{enumerate}[leftmargin=1.6em]
  \item \textbf{DSB バリア追加。} 非キャッシュ書き込みもライトバッファに
        滞留し得るため、DMA 起動前に DSB で RAM へ排出。\textbf{必要だが
        不十分}——単独では USB は復活しなかった。

  \item \textbf{\texttt{packet\_count} オーバーフロー修正。} \texttt{packet\_count}
        は 10 ビット（最大 1023）で、バウンスアリーナを 128\,KB に拡大した際に、
        小さい \texttt{max\_packet\_size} で桁溢れし得ることを発見。しかし
        \textbf{赤鰊だった}——スプリット転送では \texttt{transfer.size} が
        \texttt{max\_packet\_size} に事前制限され \texttt{packet\_count} は
        常に 1 で溢れない。撤回した。

  \item \textbf{スプリット転送ロジックの精査。} Pi 3 は全転送がスプリット
        （ハブ背後）なので、バウンスとスプリット再試行の相互作用を徹底
        トレースした。\textbf{ロジックは正しかった}——\texttt{dma\_address} の
        保持、再アーム、コピーバックのオフセットはすべて整合していた。

  \item \textbf{アリーナ非キャッシュ化の SMP タイミング修正。} 最有力仮説。
        非キャッシュ化を実行時（\texttt{usbinit} 内）に、しかも現在コアだけ
        TLB 無効化で行っていたが、SMP のワーカーコアはそれ\textbf{以前}に
        アリーナがキャッシュ可能なまま起動しており、コア間でマッピングが
        不整合だった。アリーナのアドレスはコンパイル時に確定するので、
        \texttt{mmu\_init} で全コア起動前・D-cache 有効化前に非キャッシュを
        ベイクするよう変更した。しかし\textbf{これも USB を復活させなかった}。
\end{enumerate}

\subsubsection*{結論：本番化は USB DMA に阻まれた}

四つの修正を尽くしても USB は動かなかった。バウンスロジックとスプリット
処理は精査済みで正しく、コヒーレンシ機構も（HDMI が証明する通り）機能して
いる。それでも USB が全滅する——真因は、これらより深い、実機レベルの精密
診断（USB 列挙の詳細なシリアルログ等）を要する相互作用にある。手元の
シリアルアダプタ（Prolific、macOS で不安定）ではそこまで追えなかった。

したがって本レポートの範囲では、\textbf{D-cache は計算と表示には本番投入
できるが、USB DMA が最後の、そして越えられない壁である}と結論する。第
\ref{sec:dcache-usb} 項の構造的予測——USB のバッファ出自が本質的な障害——が、
バウンスという迂回策すら退ける形で、より強く裏付けられた。8.8 倍の単一コア
高速化という果実は実在するが、それを本番で収穫するには、USB ドライバの
DMA 経路をより根本的に作り直す必要がある。これは明確に次段階の課題である。

\subsection{ボード横断の追試：Cortex-A76（Pi 5）でも多コア D-cache は成立する}
\label{sec:dcache-a76}

同じ枠組みを Raspberry Pi 5（Cortex-A76、AArch64、BCM2712）へ移植し、A72 と同じ
\textbf{明示キャッシュ管理}（\texttt{dc cvac/ivac}、第 \ref{sec:dcache-a72-multi} 項）を
\texttt{smp.c} に入れて、\textbf{同一ブート内で OFF（単一コア）→ C=1 →
多コア ON を連続測定}した（\bench{dining} が OFF 95{,}501 対 ON 95{,}501\,\textmu s と
一致し、同一クロックが保証される）。

\subsubsection*{単一コア：ここでも再帰は跳ね、書込ストリームは動かない}

\begin{table}[htbp]
\centering
\caption{Pi 5（Cortex-A76）単一コア、D-cache OFF vs ON（同一ブート・同一クロック）}
\label{tab:dcache-a76-single}
\small
\begin{tabular}{@{}llccc@{}}
\toprule
ベンチ & 性質 & OFF & ON & 高速化 \\
\midrule
\bench{nqueens} $n{=}12$ & 再帰・深いスタック & 109{,}278\,\textmu s & 6{,}988\,\textmu s & \textbf{\spd{15.6}} \\
\bench{dining}           & 計算・微小作業セット & 95{,}501\,\textmu s & 95{,}501\,\textmu s & \spd{1.00} \\
\bench{fill} 1\,MB       & 書込ストリーム & 1{,}364\,\textmu s & 1{,}338\,\textmu s & \spd{1.02} \\
\bottomrule
\end{tabular}
\end{table}

\noindent
A76 の単一コア D-cache 像は A72（第 \ref{sec:dcache-a72} 節）と同じ形をしている：
\bench{nqueens} が \textbf{\spd{15.6}}（3 世代で最大）と跳ね、\bench{fill}（書込
ストリーム）はほぼ不変である。\textbf{ここで従来の記述を訂正する}：以前は A76 の
\bench{fill} 1\,MB を「\spd{1.55} 高速」としていたが、それは\textbf{複数ブートを
混ぜた予備捕捉のクロック差}によるもので、\textbf{同一クロックで測り直すと \spd{1.02}}
（1{,}364 対 1{,}338\,\textmu s）に過ぎない。fill は D-cache 向きの
ワークロードではない——という A72 の結論は A76 でも成立する。

\subsubsection*{多コア：明示キャッシュ管理で成立（全 \texttt{agree=yes}）}

以前は A76 の多コア D-cache を「崩れた（\spd{1.0}、予備観測）」と記していた。
だが A72 で有効だった\textbf{明示キャッシュ管理}（メールボックスと二次コアの
起動データを \texttt{dc cvac/ivac} で同期）を A76 にも入れると、\textbf{多コアは
成立した}——全ベンチ \texttt{agree=yes}、\bench{dining} は理想の \spd{3.99}。

\begin{table}[htbp]
\centering
\caption{Pi 5（A76）多コア D-cache（\texttt{C=1}、明示キャッシュ管理、\texttt{cores\_online=4}）}
\label{tab:dcache-a76-multi}
\small
\begin{tabular}{@{}lcccl@{}}
\toprule
ベンチ & 1 コア & 4 コア & 速度向上 & \\
\midrule
\bench{dining}        & 95{,}501\,\textmu s & 23{,}878\,\textmu s & \spd{3.99} & \texttt{agree=yes} \\
\bench{nqueens} $n{=}12$ & 6{,}988\,\textmu s & 1{,}831\,\textmu s & \spd{3.81} & \texttt{agree=yes} \\
\bench{fill} 16\,KB   & 83\,\textmu s & 29\,\textmu s & \spd{2.86} & キャッシュ内 \\
\bench{fill} 1\,MB    & 1{,}338\,\textmu s & 1{,}970\,\textmu s & \spd{0.67} & 共有バッファ競合 \\
\bottomrule
\end{tabular}
\end{table}

\noindent
以前の \spd{1.0} は DSU に任せてキャッシュ管理を書かなかったための非コヒーレンシで
あり、\textbf{ページ shareability の問題というより、明示的な clean/invalidate が
無かったこと}が主因だった。A76 の DSU は inner-shareable WB を扱うが、本移植の
ロックフリー・メールボックスを確実にコヒーレントにするには A72 と同じ手当てで
足りる。\bench{nqueens} は D-cache ON で \spd{3.81} までスケールし（OFF の静的分割
\spd{1.5} 級から改善——A72 と同じく RAM スタック競合が C=1 で解けた）、単一コア
\spd{15.6} と合わせて \textbf{OFF 単一コア 109{,}278\,\textmu s → ON 4 コア
1{,}831\,\textmu s ＝ 約 \spd{59.7}}に達する。一方 \bench{fill} 1\,MB は \spd{0.67}
と多コアで\textbf{遅くなる}（共有バッファへの書き戻し競合）——「メモリ律速は
スケールしない」は A76・多コア・D-cache ON でも健在である。

\medskip
\noindent
三世代の総括（第 \ref{tab:dcache-crossgen} 表）：\textbf{多コア D-cache は 3 世代とも
達成可能}だが手段が違う——A53 は \texttt{SMPEN}、A72・A76 は\textbf{明示キャッシュ
管理という共通の手当て}で成立した。ボードごとにコヒーレンシ管理の微アーキ依存性が
姿を変える事実は、「キャッシュ OFF なら全アクセスが RAM 直行で自動的にコヒーレント」
という\textbf{ワーカープール＋D-cache OFF が移植の現実解}という中心的主張（第
\ref{sec:design} 節）を、むしろ裏づける。D-cache の利得（再帰で \spd{15.6}）は
本物だが、それを多コアで正しく収穫するにはボード固有の手当てが要る。

\subsection{ボード横断の追試：Cortex-A72（Pi 4）——SMPEN が EL1 から届かない}
\label{sec:dcache-a72}

同じ \texttt{DCACHE\_EXPERIMENT} 変種を三つ目の微アーキテクチャ、Raspberry Pi 4
（BCM2711、Cortex-A72、AArch64）へ移植した。A72 は\textbf{位置づけとして A53 と
A76 の中間}にある——A53（Pi 3）と同じく \texttt{CPUECTLR\_EL1.SMPEN} を持つ一方、
A76（Pi 5）と同じ「クアッドコア＋共有 L2」構成である。したがって問いは：
\textbf{A72 の SMPEN は、A53 と同じくコア間コヒーレンシをきれいに与えるか}。
結論から言えば、A72 では\textbf{そもそも SMPEN に手が届かず}、これが本レポートで
最も根源的な D-cache 障害として現れた。

\subsubsection*{障害の梯子：HDMI・SMPEN・二次コア}

シリアルアダプタ（Prolific PL2303）が macOS で \texttt{tcsetattr} を受け付けず
使用不能だったため、A72 では\textbf{結果を HDMI 画面に出して撮影}する経路を採った。
D-cache ON では画面出力自体が三段の罠を踏み、順に解いた。

\begin{enumerate}[leftmargin=1.6em]
  \item \textbf{フレームバッファ・メールボックスの非コヒーレンシ。}
        \texttt{mmu\_init} で早期に \texttt{C=1} にすると、直後の \texttt{video\_init}
        が VideoCore へ送る property-mailbox 要求が\textbf{キャッシュに滞留}し、GPU が
        stale な RAM を読んで FB 確保に失敗——画面は真っ暗のままになる（第
        \ref{sec:dcache-usb} 項で Pi 3 が踏んだ罠と同一）。対策として D-cache の
        有効化を\textbf{\texttt{video\_init} の後ろへ遅延}させ、HDMI を \texttt{C=0} で
        立ち上げてから \texttt{C=1} に切り替える構成にした（加えて \texttt{mbox\_call}
        に要求のクリーン／応答のインバリデートを挿入）。以後、各段階を画面に印字し
        \texttt{dc cisw} で FB を RAM へ書き戻すことで、進捗が撮影可能になった。

  \item \textbf{SMPEN が EL1 から書けない（本命）。}
        Pi 4 のファームウェアはコアを EL2 に落とし、\texttt{boot.S} が EL2$\to$EL1 に
        遷移させる——カーネルは \textbf{EL1} で走る。ところが A72 では、IMP-DEF の
        \texttt{CPUECTLR\_EL1}（\texttt{S3\_1\_C15\_C2\_1}）は
        \texttt{ACTLR\_EL2/EL3.CPUECTLR} が上位 EL で許可されていない限り
        \textbf{EL1 からアクセスできない}。Pi 4 ファームはこれを許可しないため、
        \texttt{SMPEN} を立てる \texttt{MRS} が\textbf{未設定の EL2 ベクタ
        （\texttt{VBAR\_EL2}=0）へトラップし、コアはアドレス 0 へ暴走}する。
        画面は \texttt{[dcache] enabling C=1} の手前で凍結した。SMPEN 抜きでは
        \texttt{C=1} にしても\textbf{コア間コヒーレンシが成立しない}。

  \item \textbf{二次コアの起動窓（Pi 3 と同じ罠）。}
        SMPEN を諦めて \texttt{C=1} だけにすると有効化自体は\textbf{通った}
        （\texttt{SCTLR}=\texttt{0x30d01805}、bit 2 立ち、brick なし）。だが今度は
        \texttt{smp\_init} が停止した。\texttt{C=1} のコア 0 が二次コアのスタック
        ポインタと解放語を書いても\textbf{ダーティ・キャッシュに留まり}、キャッシュ
        OFF で起動する二次コアは stale な RAM を読んで死ぬ（第 \ref{sec:dcache-usb}
        項の罠と同型）。SMPEN が無い以上コヒーレンシは本質的に張れないので、
        本追試は\textbf{単一コアで}測ることにした（\texttt{smp\_init} を早期リターン）。
\end{enumerate}

\noindent
まとめると、\textbf{A72 の多コア D-cache は、A76 のような属性設定の不備ではなく、
「SMPEN に EL1 から触れない」というより下層（例外レベル／ファームウェア）の壁で
塞がれている}。これを越えるには \texttt{boot.S} の EL2 区間で SMPEN を書く（あるいは
ファーム側で \texttt{ACTLR} を開ける）改修が要り、本実験の範囲外である。

\subsubsection*{単一コア：D-cache ON の利得はワークロード依存（再帰で \spd{14.7}、書込ストリームで \spd{1.0}）}

単一コアで \texttt{C=0} と \texttt{C=1} を\textbf{同一ブート内で連続測定}した
（ファームのクロック・ランプによる誤差を避けるため）。\bench{dining} の絶対時間が
両者で 91{,}022 対 91{,}002\,\textmu s と\textbf{ほぼ一致}することが、この 2 回が
\textbf{同一クロックで走った}動かぬ証拠である（クロックが変われば計算律速の
\bench{dining} が真っ先にずれる）。

\begin{table}[htbp]
\centering
\caption{Pi 4（Cortex-A72）単一コア、D-cache OFF vs ON（同一ブート・同一クロック）}
\label{tab:dcache-a72-single}
\small
\begin{tabular}{@{}llccc@{}}
\toprule
ワークロード & 性質 & OFF & ON & 高速化 \\
\midrule
\bench{nqueens} $n{=}12$ & 再帰・深いスタック & 189{,}445\,\textmu s & 12{,}878\,\textmu s & \textbf{\spd{14.7}} \\
\bench{dining}           & 計算・微小作業セット & 91{,}022\,\textmu s & 91{,}002\,\textmu s & \spd{1.00} \\
\bench{fill} 4{,}096 (16\,KB)   & 書込ストリーム & 46\,\textmu s   & 38\,\textmu s   & \spd{1.21} \\
\bench{fill} 16{,}384 (64\,KB)  & 書込ストリーム & 177\,\textmu s  & 168\,\textmu s  & \spd{1.05} \\
\bench{fill} 65{,}536 (256\,KB) & 書込ストリーム & 680\,\textmu s  & 700\,\textmu s  & \spd{0.97} \\
\bench{fill} 262{,}144 (1\,MB)  & 書込ストリーム & 2{,}766\,\textmu s & 2{,}823\,\textmu s & \spd{0.98} \\
\bottomrule
\end{tabular}
\end{table}

\noindent
表 \ref{tab:dcache-a72-single} の結論は、\textbf{A72 の単一コア D-cache 利得は
アクセスパターンに強く依存し、適合するワークロードでは絶大}という点である。
\bench{nqueens}（再帰バックトラック）は D-cache ON で\textbf{\spd{14.7}}——A53（Pi 3）の
\bench{nqueens} \spd{8.8}（第 \ref{sec:dcache-result} 項）をも上回る。深い呼び出し
スタックを何度も舐める処理は、\texttt{C=0} では毎回 RAM に往復するが、\texttt{C=1} では
ホットなスタックが L1 に載り切るためである。一方 \bench{dining} は作業セットが
レジスタ／微小で不変、\bench{fill} は\textbf{書き込み専用のストリーム}で書き戻し帯域が
律速のため \spd{1.0} 前後に留まる（write-allocate 型 WB でも純書込は伸びない）。

\medskip\noindent
\textbf{これは重要な訂正でもある}：\bench{fill} だけを見れば「A72 の D-cache 利得は
乏しい（\spd{1.1}）」と誤読しかねないが、\bench{nqueens} \spd{14.7} が\textbf{D-cache は
確かに効いている}ことを証明する。fill が伸びないのはキャッシュが働いていないからでは
なく、\textbf{fill がキャッシュ向きのワークロードでないから}である。D-cache の価値は
「入れれば一律に速い」ではなく、\textbf{再帰・局所参照の多い処理でこそ跳ねる}——という
のが A72 実測の教訓である。

\subsubsection*{多コア：SMPEN 無しでも明示キャッシュ管理で成立}
\label{sec:dcache-a72-multi}

上の「障害の梯子」では、SMPEN が EL1 から立てられず\texttt{C=1} 下の多コアを
諦めた。だが\textbf{SMPEN は本質ではなかった}——必要なのはコア間の\textbf{コヒーレンシ}
であって、その手段は SMPEN によるハードウェア自動スヌープに限らない。そこで、
ロックフリー・ジョブメールボックスと二次コアのブリングアップ・データ（スタック
ポインタ・解放語・online フラグ）の各同期点に、\textbf{明示的なキャッシュ管理}を
挿入した：書いたら \texttt{dc cvac} で RAM へ排出、読む前に \texttt{dc ivac} で無効化
（いずれも EL1 で合法・トラップしない）。これは \texttt{system/smp.c} に閉じた変更で、
CPUECTLR にも EL2 にも触れない。

結果、\texttt{C=1} のまま\textbf{全 4 コアが起動し（\texttt{cores\_online = 4}）、
コヒーレンシも保たれた}（全ベンチ \texttt{agree=yes}）。

\begin{table}[htbp]
\centering
\caption{Pi 4（A72）多コア D-cache（\texttt{C=1}、明示キャッシュ管理、同一ブート）}
\label{tab:dcache-a72-multi}
\small
\begin{tabular}{@{}lcccl@{}}
\toprule
ベンチ & 1 コア & 4 コア & 4 コア速度向上 & \\
\midrule
\bench{dining}        & 91{,}002\,\textmu s & 22{,}754\,\textmu s & \spd{3.99} & 完全スケール・\texttt{agree=yes} \\
\bench{nqueens} $n{=}12$ & 12{,}885\,\textmu s & 3{,}362\,\textmu s & \spd{3.83} & \texttt{agree=yes} \\
\bench{fill} 1\,MB    & 2{,}792\,\textmu s & 3{,}177\,\textmu s & \spd{0.87} & 共有バッファ＝書戻し競合 \\
\bottomrule
\end{tabular}
\end{table}

\noindent
\textbf{これは二重に重要な結果である}。第一に、\bench{dining} \spd{3.99}
（\texttt{agree=yes}）は、\textbf{A72 の多コア D-cache コヒーレンシが SMPEN 無しでも
成立する}ことを示す——「A72 は多コアの入口で塞がれた」という上の結論は、
\textbf{ハードウェア自動コヒーレンシ}に限れば正しいが、\textbf{ソフトウェアの
明示キャッシュ管理}で越えられた。第二に、\bench{nqueens} が \textbf{D-cache ON では
\spd{3.83} までスケール}した点である——D-cache \textbf{OFF} の \spd{2.0}（第
\ref{sec:cross-fair} 項）とは大きく違う。すなわち\textbf{OFF での \spd{2.0} 頭打ちは
負荷不均衡だけが原因ではなく、深い再帰スタックの RAM 競合も効いていた}：\texttt{C=1}
では各コアのスタックが L1 に私有化されコア間競合が消えるため、静的分割でも
ほぼ線形に伸びる。\bench{nqueens} の総合利得は、D-cache ON・4 コアの 3{,}362\,\textmu s
対 D-cache OFF・単一コアの 189{,}458\,\textmu s で\textbf{約 \spd{56}}に達する。
一方 \bench{fill}（共有の書込専用バッファ）は \spd{0.87} と\textbf{むしろ遅く}、
複数コアが同一バッファへ書くと write-back 競合が生じる——「メモリ律速はスケール
しない」は D-cache ON・多コアでも（別機序で）健在である。

\subsubsection*{三世代の総合：D-cache は「入れれば速い」ではない}

三つの微アーキテクチャで、D-cache ON はそれぞれ\textbf{異なる壁}を見せた。

\begin{table}[htbp]
\centering
\caption{D-cache ON の世代横断まとめ}
\label{tab:dcache-crossgen}
\small
\begin{tabular}{@{}llll@{}}
\toprule
 & A53 / Pi 3 & A72 / Pi 4 & A76 / Pi 5 \\
\midrule
コヒーレンシ機構 & \texttt{SMPEN}(EL1可) & \texttt{SMPEN}(EL1不可) & DSU \\
多コア D-cache   & 成立（\bench{dining} \spd{3.99}） & \textbf{成立}（明示キャッシュ管理） & \textbf{成立}（明示キャッシュ管理） \\
単一コア \bench{nq} & 大 \spd{8.8} & 大 \spd{14.7} & \textbf{大 \spd{15.6}} \\
多コア \bench{nq} & \spd{3.65}(SMPEN) & \spd{3.83}(dc cvac/ivac) & \spd{3.81}(dc cvac/ivac) \\
\bench{dining} 多コア & \spd{3.99} & \spd{3.99} & \spd{3.99} \\
最後の壁         & USB DMA の出自 & USB DMA（本番化） & 共有バッファ書戻し（\bench{fill}） \\
\bottomrule
\end{tabular}
\end{table}

\noindent
\textbf{三世代とも多コア D-cache は達成できた}——A53 は \texttt{SMPEN}、A72 と A76 は
\textbf{明示的なキャッシュ管理（\texttt{dc cvac/ivac}）という共通の手当て}で成立させた
（いずれも \bench{dining} \spd{3.99}・全 \texttt{agree=yes}）。当初 A72 は「SMPEN が
EL1 から届かず不成立」、A76 は「DSU 任せで \spd{1.0}」と見えたが、どちらも真因は
\textbf{ロックフリー・メールボックスにソフトウェアの clean/invalidate を書かなかった
こと}であり、同一の手当てで解けた。ただし\textbf{手段はボード世代ごとに異なる下層の
約束事に依存する}（A53=SMPEN の有無、A72=EL1 特権、A76=DSU/shareability）。この事実は
「キャッシュ OFF なら全アクセスが RAM 直行で\textbf{自動的にコヒーレント}」という
\textbf{ワーカープール＋D-cache OFF が移植の現実解である}という本レポートの中心的主張
（第 \ref{sec:design} 節）を、三世代分の独立した証拠で\textbf{むしろ強く裏づける}：
D-cache の利得（単一コア \bench{nq} は A72 \spd{14.7}／A76 \spd{15.6}、多コアと掛けて
\spd{56}--\spd{60}）は本物だが、それを多コアで正しく収穫するには世代ごとに異なる下層の
約束事を履行せねばならず、その工数とリスクが「まず D-cache OFF で正しく動かす」判断を
裏打ちする。
